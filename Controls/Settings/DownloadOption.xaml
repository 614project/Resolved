<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Resolved.Controls.Settings.DownloadOption"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Resolved.Controls.Settings"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="using:CommunityToolkit.WinUI.Controls"
    mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <ProgressBar Grid.Column="0" Visibility="Collapsed" x:Name="MyProgress" Margin="0,0,8,0"/>
        <Button Grid.Column="1" FontFamily="Outfit" Content="Download start" x:Name="MyButton" Click="DownloadStart" ToolTipService.ToolTip="Start download problems. (can't cancel while downloding)"/>
    </Grid>
</UserControl>
