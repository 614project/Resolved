<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Resolved.Controls.Settings.ThemeOption"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Resolved.Controls.Settings"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="using:CommunityToolkit.WinUI.Controls"
    xmlns:opt="using:Resolved.Controls.Settings"
    mc:Ignorable="d">
    <ui:SettingsCard Header="Theme" Description="Change the colors that light or dark." FontFamily="Outfit">
        <ComboBox FontFamily="Outfit" x:Name="ThemeComboBox" Loaded="ThemeComboBoxLoaded" SelectionChanged="ThemeComboBoxSelectionChanged">
            <ComboBoxItem Content="System" ToolTipService.ToolTip="Follow system default settings." FontFamily="Outfit"/>
            <ComboBoxItem Content="Light" ToolTipService.ToolTip="Set a light theme." FontFamily="Outfit"/>
            <ComboBoxItem Content="Dark" ToolTipService.ToolTip="Set a dark theme." FontFamily="Outfit"/>
        </ComboBox>
    </ui:SettingsCard>
</UserControl>
