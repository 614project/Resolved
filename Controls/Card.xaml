<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Resolved.Controls.Card"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Resolved.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="using:CommunityToolkit.WinUI.Controls"
    mc:Ignorable="d">
    <ui:SettingsCard Content="{x:Bind Content, Mode=OneWay}">
        <ui:SettingsCard.Header>
            <TextBlock Text="{x:Bind Title, Mode=OneWay}"/>
        </ui:SettingsCard.Header>
        <ui:SettingsCard.Description>
            <TextBlock Text="{x:Bind Description, Mode=OneWay}" />
        </ui:SettingsCard.Description>
    </ui:SettingsCard>
</UserControl>
