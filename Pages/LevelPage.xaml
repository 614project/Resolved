<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Resolved.Pages.LevelPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Resolved.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Grid HorizontalAlignment="Stretch" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock FontFamily="Outfit" Text="Levels" FontSize="20" Grid.Column="0"/>
        </Grid>
        <ScrollView Grid.Row="1" Background="{ThemeResource TranslucentListViewBackgroundColor}">
            <ListView ItemsSource="{x:Bind LevelDataCollection}">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="local:LevelFieldData">
                        <Grid ColumnSpacing="4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="110"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock FontFamily="Outfit" Text="{x:Bind Name}" Foreground="{x:Bind Color}" Grid.Column="0"/>
                            <TextBlock FontFamily="Outfit" Text="{x:Bind Total}" Grid.Column="1"/>
                            <TextBlock FontFamily="Outfit" Text="{x:Bind Solved}" Grid.Column="2"/>
                            <TextBlock FontFamily="Outfit" Text="{x:Bind Unsolved}" Grid.Column="3"/>
                            <ProgressBar Value="{x:Bind SolvedPercentage}" Grid.Column="4"/>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </ScrollView>
    </Grid>
</Page>
